# =============================================================================
# airymax-agents-rs（Rust 工作区，位于 airymax-agents 仓内）
# 策略：非必要不追踪。仅忽略本地构建产物，源码与必要配置保持可追踪
# =============================================================================

# Rust 构建产物
/target
**/target/

# Rust 分析
**/*.rs.bk

# 注：工作区含二进制 crate（coding_agent 等），Cargo.lock 需入库
# 以保证可复现构建，故不忽略
